class LottoResult {
  compare(userNumber, winningNumber) {
    return winningHistory;
  }

  calcLottoReturn(winningHistory) {
    return lottoReturn;
  }

  printCompareResult() {}

  printCalcResult() {}
}
